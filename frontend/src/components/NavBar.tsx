import { Button } from '@mantine/core';
const NavBar = () => {
    return (
        <>
            <Button>Fest Admin</Button>
            <p className="text-xl"></p>
        </>
    );
};

export default NavBar;
